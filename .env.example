# ===========================================
# Frontend (Vite) – optional for local dev
# ===========================================
# Copy to .env and adjust. Only VITE_* vars are exposed to the client.

# API base URL. Leave unset in dev to use /api (proxied to backend). Set when using a separate backend.
VITE_API_BASE_URL=http://localhost:3000/api

# Canonical site URL for SEO (optional). Used by config/site.ts. Fallback: current origin or nigeriaelection.com
# VITE_SITE_URL=https://www.nigeriaelection.com

# ===========================================
# Backend uses backend/.env (see backend/.env.example)
# When you run: cd backend && pnpm dev  →  backend/.env is loaded
# ===========================================
